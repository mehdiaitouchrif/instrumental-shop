import Layout from "../components/Layout";
import Collections from "../components/Collections";

const Home = () => {
  return (
    <Layout>
      <Collections />
    </Layout>
  );
};

export default Home;
